# DNS resolver для динамического resolve в upstream-блоках.
# По умолчанию — встроенный DNS Docker (docker-compose).
# Для Kubernetes переопределите этот файл, указав адрес kube-dns (например, 10.96.0.10).
resolver 127.0.0.11 valid=10s ipv6=off;
resolver_timeout 2s;
