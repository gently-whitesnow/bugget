openapi: 3.0.3

# Контракт модуля Users — та его часть, которую зовёт фронт.
# Источник правды для Users.Api/Generated/*.g.cs.
#
# Пользовательские пути объявлены только с контекстом
# `/v1/workspaces/{workspaceId}/teams/{teamId}/...` — по ним ходит фронт.
# Бесконтекстные дубли (`/v1/users/**`) и внутренние ручки (`/_internal/users/**`)
# удалены в MAIN-17: потребителей у них не осталось. Суффикс `InContext`
# в operationId сохранён, чтобы не переименовывать сгенерированные методы.
#
# Правится этот файл, а не .g.cs: перегенерация — scripts/quality/openapi-generate.sh.

info:
  title: Bugget Users API
  version: 1.0.0
  description: |-
    Рабочие пространства, команды, участники, приглашения и профиль пользователя.

paths:


  /v1/workspaces/{workspaceId}/teams/{teamId}/users:
    get:
      operationId: Users_GetUserInContext
      summary: Профиль текущего пользователя.
      parameters:
        - $ref: '#/components/parameters/WorkspaceIdIgnored'
        - $ref: '#/components/parameters/TeamIdIgnored'
      responses:
        '200':
          description: Профиль.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: Пользователь не найден.
        '500':
          $ref: '#/components/responses/InternalServerError'

    put:
      operationId: Users_PutUserInContext
      summary: Обновить профиль текущего пользователя.
      parameters:
        - $ref: '#/components/parameters/WorkspaceIdIgnored'
        - $ref: '#/components/parameters/TeamIdIgnored'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserUpdateRequest'
      responses:
        '200':
          description: Профиль обновлён.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserProfile'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

    delete:
      operationId: Users_DeleteUserInContext
      summary: Удалить текущего пользователя.
      parameters:
        - $ref: '#/components/parameters/WorkspaceIdIgnored'
        - $ref: '#/components/parameters/TeamIdIgnored'
      responses:
        '200':
          description: Пользователь удалён. Тело пустое.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'


  /v1/workspaces/{workspaceId}/teams/{teamId}/users/batch/list:
    post:
      operationId: Users_ListUsersInContext
      summary: Пользователи по списку идентификаторов.
      description: |-
        Нераспознанные идентификаторы просто не вернутся. Пустой результат —
        пустой массив, а не 404.
      parameters:
        - $ref: '#/components/parameters/WorkspaceIdIgnored'
        - $ref: '#/components/parameters/TeamIdIgnored'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IdList'
      responses:
        '200':
          description: Найденные пользователи.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/User'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'


  /v1/workspaces/{workspaceId}/teams/{teamId}/users/autocomplete:
    get:
      operationId: Users_AutocompleteUsersInContext
      summary: Поиск пользователей по имени в текущем рабочем пространстве.
      parameters:
        - $ref: '#/components/parameters/WorkspaceIdIgnored'
        - $ref: '#/components/parameters/TeamIdIgnored'
        - name: searchString
          in: query
          required: false
          description: Строка поиска по имени.
          schema:
            type: string
            nullable: true
        - name: skip
          in: query
          required: false
          description: Сколько записей пропустить.
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: take
          in: query
          required: false
          description: Размер страницы.
          schema:
            type: integer
            default: 10
            minimum: 1
            maximum: 100
      responses:
        '200':
          description: Подходящие пользователи.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutocompleteUsers'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'


  /v1/workspaces/{workspaceId}/teams/{teamId}/users/external-links:
    get:
      operationId: Users_GetExternalLinksInContext
      summary: Привязанные способы входа текущего пользователя.
      parameters:
        - $ref: '#/components/parameters/WorkspaceIdIgnored'
        - $ref: '#/components/parameters/TeamIdIgnored'
      responses:
        '200':
          description: Привязки.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ExternalLink'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'


  /v1/workspaces/{workspaceId}/teams/{teamId}/users/external-links/{provider}:
    delete:
      operationId: Users_UnlinkProviderInContext
      summary: Отвязать способ входа.
      description: |-
        Единственный способ входа отвязать нельзя — 400 с кодом `last_login_method`.
      parameters:
        - $ref: '#/components/parameters/WorkspaceIdIgnored'
        - $ref: '#/components/parameters/TeamIdIgnored'
        - $ref: '#/components/parameters/Provider'
      responses:
        '204':
          description: Способ входа отвязан.
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: Такой привязки у пользователя нет.
        '500':
          $ref: '#/components/responses/InternalServerError'


  /v1/workspaces/{workspaceId}/teams/{teamId}/users/personal-access-tokens:
    get:
      operationId: PersonalAccessTokens_ListInContext
      summary: Personal access tokens текущего пользователя.
      description: |-
        Токены пользователя по всем его командам, а не только по команде из адреса:
        это собственные данные пользователя, и при переключении команды они не должны
        «пропадать» из настроек. Область действия каждого токена видна в самой строке
        (`workspace_id`, `team_id`). Секрет в ответе не встречается ни в каком виде —
        только опознавательный префикс.
      parameters:
        - $ref: '#/components/parameters/WorkspaceIdIgnored'
        - $ref: '#/components/parameters/TeamIdIgnored'
      responses:
        '200':
          description: Неотозванные токены, свежие сверху. Просроченные остаются в
            списке — пользователь должен видеть, что именно истекло.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PersonalAccessToken'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      operationId: PersonalAccessTokens_CreateInContext
      summary: Выпустить personal access token.
      description: |-
        Токен привязывается к рабочему пространству и команде из identity текущего
        запроса и наследует права владельца в этой команде. Значение токена возвращается
        только из этой ручки и только один раз: в хранилище остаётся хэш, повторно
        показать значение нечем.
      parameters:
        - $ref: '#/components/parameters/WorkspaceIdIgnored'
        - $ref: '#/components/parameters/TeamIdIgnored'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PersonalAccessTokenCreateRequest'
      responses:
        '200':
          description: Токен выпущен. Единственный ответ, содержащий его значение.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonalAccessTokenCreated'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: В identity запроса нет команды — токен не к чему привязать.
        '500':
          $ref: '#/components/responses/InternalServerError'


  /v1/workspaces/{workspaceId}/teams/{teamId}/users/personal-access-tokens/{tokenId}:
    delete:
      operationId: PersonalAccessTokens_RevokeInContext
      summary: Отозвать personal access token.
      description: |-
        Отзыв необратим и действует немедленно. Отозвать можно только собственный
        токен: чужой, уже отозванный и несуществующий неразличимы — 404.
      parameters:
        - $ref: '#/components/parameters/WorkspaceIdIgnored'
        - $ref: '#/components/parameters/TeamIdIgnored'
        - $ref: '#/components/parameters/TokenId'
      responses:
        '204':
          description: Токен отозван.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: Токена нет, он чужой или уже отозван.
        '500':
          $ref: '#/components/responses/InternalServerError'


  /v1/workspaces/{workspaceId}/teams/{teamId}/users/merge:
    post:
      operationId: Users_MergeUsersInContext
      summary: Объединить аккаунты.
      description: |-
        Переносит данные исходного аккаунта в текущий.
      parameters:
        - $ref: '#/components/parameters/WorkspaceIdIgnored'
        - $ref: '#/components/parameters/TeamIdIgnored'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MergeUsersRequest'
      responses:
        '200':
          description: Аккаунты объединены. Тело пустое.
        '400':
          description: Некорректный идентификатор либо аккаунт объединяют сам с собой.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: Исходный аккаунт не найден.
        '409':
          description: Исходный аккаунт владеет рабочими пространствами.
        '500':
          $ref: '#/components/responses/InternalServerError'


  /v1/workspaces/{workspaceId}/teams/{teamId}/users/mattermost:
    put:
      operationId: Users_LinkMattermostInContext
      summary: Привязать аккаунт Mattermost вручную.
      parameters:
        - $ref: '#/components/parameters/WorkspaceIdIgnored'
        - $ref: '#/components/parameters/TeamIdIgnored'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LinkMattermostRequest'
      responses:
        '204':
          description: Аккаунт привязан.
        '400':
          description: Некорректный идентификатор Mattermost.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

    delete:
      operationId: Users_UnlinkMattermostInContext
      summary: Отвязать аккаунт Mattermost.
      parameters:
        - $ref: '#/components/parameters/WorkspaceIdIgnored'
        - $ref: '#/components/parameters/TeamIdIgnored'
      responses:
        '200':
          description: Аккаунт отвязан. Тело пустое.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'


  /v1/workspaces/{workspaceId}/teams/{teamId}/users/avatar:
    post:
      operationId: Avatar_UploadAvatarInContext
      summary: Загрузить свой аватар.
      description: |-
        Не больше 200 КБ, форматы JPEG, PNG, GIF и WebP.
      parameters:
        - $ref: '#/components/parameters/WorkspaceIdIgnored'
        - $ref: '#/components/parameters/TeamIdIgnored'
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AvatarUpload'
      responses:
        '200':
          description: Аватар загружен. Тело пустое.
        '400':
          description: Файл больше лимита либо формат не поддерживается.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

    delete:
      operationId: Avatar_DeleteAvatarInContext
      summary: Удалить свой аватар.
      parameters:
        - $ref: '#/components/parameters/WorkspaceIdIgnored'
        - $ref: '#/components/parameters/TeamIdIgnored'
      responses:
        '204':
          description: Аватар удалён.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'


  /v1/workspaces/{workspaceId}/teams/{teamId}/users/avatar/content:
    get:
      operationId: Avatar_GetAvatarContentInContext
      summary: Содержимое своего аватара.
      parameters:
        - $ref: '#/components/parameters/WorkspaceIdIgnored'
        - $ref: '#/components/parameters/TeamIdIgnored'
      responses:
        '200':
          $ref: '#/components/responses/AvatarContent'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: Аватара нет.
        '500':
          $ref: '#/components/responses/InternalServerError'


  /v1/workspaces/{workspaceId}/teams/{teamId}/users/{userId}/avatar/content:
    get:
      operationId: Avatar_GetUserAvatarContentInContext
      summary: Содержимое аватара пользователя из текущего рабочего пространства.
      parameters:
        - $ref: '#/components/parameters/WorkspaceIdIgnored'
        - $ref: '#/components/parameters/TeamIdIgnored'
        - $ref: '#/components/parameters/UserId'
      responses:
        '200':
          $ref: '#/components/responses/AvatarContent'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: Пользователь не найден либо у него нет аватара.
        '500':
          $ref: '#/components/responses/InternalServerError'


  /v1/workspaces:
    post:
      operationId: Workspaces_CreateWorkspace
      summary: Создать рабочее пространство.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorkspaceCreateRequest'
      responses:
        '200':
          description: Рабочее пространство создано.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Workspace'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'
    get:
      operationId: Workspaces_GetWorkspacesContext
      summary: Рабочие пространства пользователя со всем контекстом.
      description: |-
        Один запрос на весь стартовый экран: пространства с командами, членство
        в пространствах и членство в командах.
      responses:
        '200':
          description: Контекст пользователя.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspacesContext'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /v1/workspaces/{workspaceId}:
    put:
      operationId: WorkspacesAdmin_UpdateWorkspace
      summary: Переименовать рабочее пространство.
      parameters:
        - $ref: '#/components/parameters/WorkspaceId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorkspaceUpdateRequest'
      responses:
        '200':
          description: Рабочее пространство обновлено.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Workspace'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      operationId: WorkspacesAdmin_DeleteWorkspace
      summary: Удалить рабочее пространство.
      description: |-
        Удаляется текущее пространство пользователя, а не то, что в пути:
        идентификатор в адресе оставлен ради формы URL.
      parameters:
        - $ref: '#/components/parameters/WorkspaceIdIgnored'
      responses:
        '200':
          description: Рабочее пространство удалено. Тело пустое.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /v1/workspaces/{workspaceId}/members/join:
    post:
      operationId: WorkspaceMembers_JoinWorkspace
      summary: Вступить в рабочее пространство.
      parameters:
        - $ref: '#/components/parameters/WorkspaceId'
      responses:
        '200':
          description: Пользователь вступил.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceMember'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /v1/workspaces/{workspaceId}/teams:
    post:
      operationId: TeamsAdmin_CreateTeam
      summary: Создать команду.
      parameters:
        - $ref: '#/components/parameters/WorkspaceId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TeamCreateRequest'
      responses:
        '200':
          description: Команда создана.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Team'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /v1/workspaces/{workspaceId}/teams/{teamId}:
    put:
      operationId: TeamsAdmin_UpdateTeam
      summary: Переименовать команду.
      parameters:
        - $ref: '#/components/parameters/WorkspaceId'
        - $ref: '#/components/parameters/TeamId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TeamUpdateRequest'
      responses:
        '200':
          description: Команда обновлена.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Team'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      operationId: TeamsAdmin_DeleteTeam
      summary: Удалить команду.
      parameters:
        - $ref: '#/components/parameters/WorkspaceId'
        - $ref: '#/components/parameters/TeamId'
      responses:
        '200':
          description: Команда удалена. Тело пустое.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /v1/workspaces/{workspaceId}/teams/batch/list:
    post:
      operationId: Teams_ListTeams
      summary: Команды по списку идентификаторов.
      description: |-
        Нераспознанные идентификаторы просто не вернутся.
      parameters:
        - $ref: '#/components/parameters/WorkspaceId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IdList'
      responses:
        '200':
          description: Найденные команды.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TeamSummary'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /v1/workspaces/{workspaceId}/teams/autocomplete:
    get:
      operationId: Teams_AutocompleteTeams
      summary: Поиск команд по имени в рабочем пространстве.
      parameters:
        - $ref: '#/components/parameters/WorkspaceId'
        - name: searchString
          in: query
          required: false
          description: Строка поиска по имени.
          schema:
            type: string
            nullable: true
        - name: skip
          in: query
          required: false
          description: Сколько записей пропустить.
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: take
          in: query
          required: false
          description: Размер страницы.
          schema:
            type: integer
            default: 10
            minimum: 1
            maximum: 100
      responses:
        '200':
          description: Подходящие команды.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutocompleteTeams'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /v1/workspaces/{workspaceId}/teams/{teamId}/members:
    get:
      operationId: TeamMembers_ListTeamMembers
      summary: Участники команды.
      parameters:
        - $ref: '#/components/parameters/WorkspaceIdIgnored'
        - $ref: '#/components/parameters/TeamId'
      responses:
        '200':
          description: Участники и лимит размера команды.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamMembers'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      operationId: TeamMembers_LeaveTeam
      summary: Выйти из команды.
      parameters:
        - $ref: '#/components/parameters/WorkspaceIdIgnored'
        - $ref: '#/components/parameters/TeamId'
      responses:
        '200':
          description: Пользователь вышел из команды. Тело пустое.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /v1/workspaces/{workspaceId}/teams/{teamId}/members/join:
    post:
      operationId: TeamMembers_JoinTeam
      summary: Вступить в команду.
      parameters:
        - $ref: '#/components/parameters/WorkspaceIdIgnored'
        - $ref: '#/components/parameters/TeamId'
      responses:
        '200':
          description: Пользователь вступил в команду. Тело пустое.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /v1/workspaces/{workspaceId}/teams/{teamId}/members/{userId}:
    delete:
      operationId: TeamMembersAdmin_DeleteTeamMember
      summary: Удалить участника команды.
      parameters:
        - $ref: '#/components/parameters/WorkspaceIdIgnored'
        - $ref: '#/components/parameters/TeamId'
        - $ref: '#/components/parameters/UserId'
      responses:
        '200':
          description: Участник удалён. Тело пустое.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'

components:

  parameters:

    WorkspaceId:
      name: workspaceId
      in: path
      required: true
      description: Идентификатор рабочего пространства.
      schema:
        type: integer

    WorkspaceIdIgnored:
      name: workspaceId
      in: path
      required: true
      description: |-
        Сегмент адреса, значение которого ручка не использует: рабочее
        пространство берётся из identity. Описан строкой, потому что до
        contract-first этот сегмент не связывался вовсе — любой мусор в нём
        доезжал до действия, а не отбивался как 400.
      schema:
        type: string

    TeamId:
      name: teamId
      in: path
      required: true
      description: Идентификатор команды.
      schema:
        type: integer

    TeamIdIgnored:
      name: teamId
      in: path
      required: true
      description: |-
        Сегмент адреса, значение которого ручка не использует: команда берётся
        из identity. Описан строкой по той же причине, что и `WorkspaceIdIgnored`.
      schema:
        type: string

    UserId:
      name: userId
      in: path
      required: true
      description: |-
        Идентификатор пользователя. Строка канонического Int64
        (см. shared.yaml `Int64String`): числом сегмент терял бы точность
        у единственного клиента API.
      schema:
        $ref: '../shared.yaml#/components/schemas/Int64String'

    Provider:
      name: provider
      in: path
      required: true
      description: Провайдер входа, например `mattermost`.
      schema:
        type: string

    TokenId:
      name: tokenId
      in: path
      required: true
      description: |-
        Идентификатор personal access token. Строка канонического Int64
        (см. shared.yaml `Int64String`) — по той же причине, что и `UserId`.
      schema:
        $ref: '../shared.yaml#/components/schemas/Int64String'

  schemas:

    IdList:
      type: array
      description: |
        Идентификаторы строками — от 1 до 1000 за запрос. Строки, а не числа:
        так это уже принимает фронт.
      minItems: 1
      maxItems: 1000
      items:
        type: string

    WorkspaceCreateRequest:
      type: object
      description: Тело создания рабочего пространства.
      required:
        - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 32
          description: Название рабочего пространства.

    WorkspaceUpdateRequest:
      type: object
      description: Новое название рабочего пространства.
      required:
        - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 32
          description: Название рабочего пространства.

    TeamCreateRequest:
      type: object
      description: Тело создания команды.
      required:
        - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 64
          description: Название команды.

    TeamUpdateRequest:
      type: object
      description: Новое название команды.
      required:
        - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 64
          description: Название команды.

    UserUpdateRequest:
      type: object
      description: Изменяемые поля профиля.
      required:
        - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 256
          description: Отображаемое имя.

    MergeUsersRequest:
      type: object
      description: Какой аккаунт влить в текущий.
      required:
        - source_user_id
      properties:
        source_user_id:
          type: string
          minLength: 1
          maxLength: 32
          description: Идентификатор исходного аккаунта.

    LinkMattermostRequest:
      type: object
      description: Идентификатор аккаунта в Mattermost.
      required:
        - mattermost_user_id
      properties:
        mattermost_user_id:
          type: string
          minLength: 1
          maxLength: 64
          description: Идентификатор пользователя в Mattermost.

    AvatarUpload:
      type: object
      description: Файл аватара. Один файл на запрос.
      required:
        - file
      properties:
        file:
          type: string
          format: binary
          description: Содержимое файла.

    Workspace:
      type: object
      description: Рабочее пространство.
      required:
        - id
        - name
        - created_at
        - updated_at
      properties:
        id:
          type: integer
          description: Идентификатор.
        name:
          type: string
          description: Название.
        created_at:
          type: string
          format: date-time
          description: Момент создания.
        updated_at:
          type: string
          format: date-time
          description: Момент последнего изменения.

    Team:
      type: object
      description: Команда.
      required:
        - id
        - workspace_id
        - name
        - created_at
        - updated_at
      properties:
        id:
          type: integer
          description: Идентификатор.
        workspace_id:
          type: integer
          description: Рабочее пространство команды.
        name:
          type: string
          description: Название.
        created_at:
          type: string
          format: date-time
          description: Момент создания.
        updated_at:
          type: string
          format: date-time
          description: Момент последнего изменения.

    TeamSummary:
      type: object
      description: |
        Команда так, как её отдают списковые ручки: идентификатор строкой и без
        привязки к пространству.
      required:
        - id
        - name
        - created_at
        - updated_at
      properties:
        id:
          type: string
          description: Идентификатор.
        name:
          type: string
          description: Название.
        created_at:
          type: string
          format: date-time
          description: Момент создания.
        updated_at:
          type: string
          format: date-time
          description: Момент последнего изменения.

    AutocompleteTeams:
      type: object
      description: Страница подсказок по командам.
      required:
        - teams
        - total
      properties:
        teams:
          type: array
          description: Подходящие команды.
          items:
            $ref: '#/components/schemas/TeamSummary'
        total:
          type: integer
          description: Сколько команд в текущей выдаче.

    WorkspaceMember:
      type: object
      description: Членство пользователя в рабочем пространстве.
      required:
        - workspace_id
        - user_id
        - role
        - created_at
      properties:
        workspace_id:
          type: integer
          description: Рабочее пространство.
        user_id:
          $ref: '../shared.yaml#/components/schemas/Int64String'
        role:
          type: string
          description: Роль в пространстве.
        created_at:
          type: string
          format: date-time
          description: Момент вступления.

    WorkspaceMemberSummary:
      type: object
      description: |
        То же членство в контексте стартового экрана: идентификаторы строками.
      required:
        - workspace_id
        - user_id
        - role
        - created_at
      properties:
        workspace_id:
          type: string
          description: Рабочее пространство.
        user_id:
          type: string
          description: Пользователь.
        role:
          type: string
          description: Роль в пространстве.
        created_at:
          type: string
          format: date-time
          description: Момент вступления.

    TeamMemberSummary:
      type: object
      description: Членство пользователя в команде.
      required:
        - team_id
        - user_id
        - created_at
      properties:
        team_id:
          type: string
          description: Команда.
        user_id:
          type: string
          description: Пользователь.
        created_at:
          type: string
          format: date-time
          description: Момент вступления.

    TeamMembers:
      type: object
      description: Участники команды и лимит её размера.
      required:
        - members
        - size_limit
      properties:
        members:
          type: array
          description: Участники.
          items:
            $ref: '#/components/schemas/TeamMemberSummary'
        size_limit:
          type: integer
          description: |
            Предельный размер команды. 0 — лимита нет (self-hosted-контур).

    WorkspaceWithTeams:
      type: object
      description: Рабочее пространство со своими командами.
      required:
        - id
        - name
        - created_at
        - updated_at
        - teams
      properties:
        id:
          type: string
          description: Идентификатор.
        name:
          type: string
          description: Название.
        created_at:
          type: string
          format: date-time
          description: Момент создания.
        updated_at:
          type: string
          format: date-time
          description: Момент последнего изменения.
        teams:
          type: array
          nullable: true
          description: Команды пространства. `null` — не запрашивались.
          items:
            $ref: '#/components/schemas/TeamSummary'

    WorkspacesContext:
      type: object
      description: Всё, что нужно стартовому экрану про пространства пользователя.
      required:
        - workspaces
        - teams_member
        - workspaces_member
      properties:
        workspaces:
          type: array
          description: Доступные пространства с командами.
          items:
            $ref: '#/components/schemas/WorkspaceWithTeams'
        teams_member:
          type: array
          nullable: true
          description: Членство пользователя в командах.
          items:
            $ref: '#/components/schemas/TeamMemberSummary'
        workspaces_member:
          type: array
          nullable: true
          description: Членство пользователя в пространствах.
          items:
            $ref: '#/components/schemas/WorkspaceMemberSummary'

    User:
      type: object
      description: Пользователь так, как его видит фронт.
      required:
        - id
        - name
        - image_url
        - workspace_role
        - mattermost_user_id
      properties:
        id:
          $ref: '../shared.yaml#/components/schemas/UserId'
        name:
          type: string
          description: Отображаемое имя.
        image_url:
          type: string
          nullable: true
          description: Ключ аватара в хранилище. `null` — аватара нет.
        workspace_role:
          type: string
          description: Роль в текущем рабочем пространстве.
        mattermost_user_id:
          type: string
          nullable: true
          description: Привязанный аккаунт Mattermost.

    UserProfile:
      type: object
      description: |
        Профиль так, как его отдаёт обновление: с внутренним
        идентификатором (`id`, канонический Int64 строкой) и внешним
        идентификатором провайдера (`external_id`).
      required:
        - id
        - external_id
        - name
        - image_url
        - mattermost_user_id
        - registration_date
        - updated_at
      properties:
        id:
          $ref: '../shared.yaml#/components/schemas/Int64String'
        external_id:
          type: string
          description: Идентификатор у провайдера входа.
        name:
          type: string
          description: Отображаемое имя.
        image_url:
          type: string
          nullable: true
          description: Ключ аватара в хранилище.
        mattermost_user_id:
          type: string
          nullable: true
          description: Привязанный аккаунт Mattermost.
        registration_date:
          type: string
          format: date-time
          description: Момент регистрации.
        updated_at:
          type: string
          format: date-time
          description: Момент последнего изменения.

    AutocompleteUser:
      type: object
      description: Пользователь в подсказках поиска.
      required:
        - id
        - name
        - image_url
      properties:
        id:
          $ref: '../shared.yaml#/components/schemas/UserId'
        name:
          type: string
          description: Отображаемое имя.
        image_url:
          type: string
          nullable: true
          description: Ключ аватара в хранилище.

    AutocompleteUsers:
      type: object
      description: Страница подсказок по пользователям.
      required:
        - users
        - total
      properties:
        users:
          type: array
          description: Подходящие пользователи.
          items:
            $ref: '#/components/schemas/AutocompleteUser'
        total:
          type: integer
          description: Сколько пользователей в текущей выдаче.

    PersonalAccessToken:
      type: object
      description: |-
        Personal access token в списке настроек: метаданные без секрета. По `token_prefix`
        токен можно опознать среди своих, но восстановить значение из него нельзя.
      required:
        - id
        - workspace_id
        - team_id
        - label
        - token_prefix
        - created_at
        - expires_at
        - last_used_at
      properties:
        id:
          $ref: '../shared.yaml#/components/schemas/Int64String'
        workspace_id:
          type: integer
          description: Рабочее пространство, на которое выпущен токен.
        team_id:
          type: integer
          description: Команда, на которую выпущен токен.
        label:
          type: string
          description: Название, заданное при выпуске.
        token_prefix:
          type: string
          description: Открытое начало значения — только чтобы опознать токен в списке.
        created_at:
          type: string
          format: date-time
          description: Момент выпуска.
        expires_at:
          type: string
          format: date-time
          nullable: true
          description: Момент истечения; `null` — бессрочный.
        last_used_at:
          type: string
          format: date-time
          nullable: true
          description: Последняя аутентификация этим токеном; `null` — не использовался.

    PersonalAccessTokenCreateRequest:
      type: object
      description: Запрос на выпуск personal access token.
      required:
        - label
      properties:
        label:
          type: string
          minLength: 1
          maxLength: 128
          description: Название токена — зачем он выпущен, например `mcp`.
        expires_at:
          type: string
          format: date-time
          nullable: true
          description: |-
            Момент истечения. Не указан или `null` — срок по умолчанию (90 дней).
            Бессрочный токен через публичный API выпустить нельзя: забытый вечный
            токен — лишний риск, а надобности в нём у клиентов контракта нет.

    PersonalAccessTokenCreated:
      type: object
      description: |-
        Ответ на выпуск токена — единственное место, где значение существует в открытом
        виде. Оно не сохраняется и повторно не показывается.
      required:
        - token
        - personal_access_token
      properties:
        token:
          type: string
          description: Полное значение токена (`bgt_pat_…`). Показывается один раз.
        personal_access_token:
          $ref: '#/components/schemas/PersonalAccessToken'

    ExternalLink:
      type: object
      description: Привязанный способ входа.
      required:
        - provider
        - external_id
        - email
        - linked_at
      properties:
        provider:
          type: string
          description: Провайдер, например `mattermost`.
        external_id:
          type: string
          description: Идентификатор пользователя у провайдера.
        email:
          type: string
          nullable: true
          description: Почта, пришедшая от провайдера.
        linked_at:
          type: string
          format: date-time
          description: Момент привязки.

  responses:

    AvatarContent:
      description: Содержимое аватара. Content-Type — по расширению файла.
      content:
        application/octet-stream:
          schema:
            type: string
            format: binary

    BadRequest:
      description: Некорректные параметры запроса.
      content:
        application/problem+json:
          schema:
            $ref: '../shared.yaml#/components/schemas/ProblemDetails'

    Forbidden:
      description: Недостаточно прав.
      content:
        application/problem+json:
          schema:
            $ref: '../shared.yaml#/components/schemas/ProblemDetails'

    Unauthorized:
      description: Запрос без действующей identity либо с недействительной сессией.
      content:
        application/problem+json:
          schema:
            $ref: '../shared.yaml#/components/schemas/ProblemDetails'

    InternalServerError:
      description: |-
        Внутренняя ошибка сервера. Тело не раскрывает деталей: `detail` для 5xx не
        публикуется, для корреляции с журналом служит `traceId`.
      content:
        application/problem+json:
          schema:
            $ref: '../shared.yaml#/components/schemas/ProblemDetails'
